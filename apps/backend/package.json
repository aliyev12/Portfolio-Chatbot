{
  "name": "@portfolio-chatbot/backend",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bun --watch src/index.ts",
    "build": "bun build src/index.ts --outdir dist --target bun",
    "start": "bun run dist/index.js",
    "test": "bun test"
  },
  "dependencies": {
    "@portfolio-chatbot/shared": "workspace:*",
    "@upstash/redis": "^1.36.1",
    "hono": "^4.6.14",
    "openai": "^6.16.0",
    "zod": "^4.3.5"
  },
  "devDependencies": {
    "@types/bun": "^1.1.13"
  }
}
