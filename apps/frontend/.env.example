# Frontend Environment Variables
# These will be bundled into the widget.js file

# API URL (optional - defaults to current host:3000)
VITE_API_URL=http://localhost:3000

# API Token - must match the backend API_TOKEN
# This will be visible in the bundled JavaScript - it's not truly secret
# Security comes from the combination of: API token + Turnstile + CORS + Rate limiting
VITE_API_TOKEN=your-random-secure-api-token

# Cloudflare Turnstile Site Key
# Get this from: https://dash.cloudflare.com/ → Turnstile
# This is the public site key (safe to expose in client-side code)
VITE_TURNSTILE_SITE_KEY=your-turnstile-site-key
