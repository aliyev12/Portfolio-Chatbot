# Server Configuration
PORT=3000

# OpenAI Configuration
OPENAI_API_KEY=sk-your-openai-api-key-here

# Upstash Redis Configuration
UPSTASH_REDIS_URL=https://your-redis-url.upstash.io
UPSTASH_REDIS_TOKEN=your-upstash-redis-token

# System Prompt (deprecated - now loaded from secret file)
# In production (Render.com): Create a secret file named "portfolio-chatbot-system-prompt.md"
# The file will be automatically loaded from /etc/secrets/portfolio-chatbot-system-prompt.md
# In development: Uses default prompt if secret file is not found
# SYSTEM_PROMPT environment variable is no longer used

# Usage Limits
MAX_MONTHLY_CONVERSATIONS=500

# Admin Secret (generate a random secure string)
ADMIN_SECRET=your-random-secure-admin-secret

# CORS Configuration (comma-separated list of allowed origins)
ALLOWED_ORIGINS=http://localhost:4321,http://localhost:3000
